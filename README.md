# H&E Segmentation

## Release Notes

- v0.1.0 - Initial release of H&E Segmentation package
    - `parse_atomx_dir()`: to parse the AtoMx directory
    - `crop_regions_from_image()`: to crop regions from images (e.g., H&E images) based on segmentation mask
